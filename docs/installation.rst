============
Installation
============

At the command line::

    $ easy_install pip
    $ pip install solution_dates

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv solution_dates
    $ pip install solution_dates